from project root: 
dfx start --background
dfx build
dfx deploy

from frontend:
npm install @dfinity/agent @dfinity/candid

npm install -D tailwindcss@3 postcss autoprefixer

install font awesome react icons

install pinata: 
npm i pinata

install typescript: 
npm install --save-dev typescript @types/react @types/react-dom

npm install react-router-dom

npx jsrepo add https://reactbits.dev/tailwind/Backgrounds/Aurora

npm i lucide-react

npm install gsap

npm install framer-motion

// candid extractor tool for debugging
cargo install candid-extractor
cargo install generate-did

dfx wallet name
dfx wallet set-name test-wallet

dfx extension install nns
dfx nns install

npm install react-masonry-css

dfx commands
    - dfx start --background
    - dfx start --clean --background
    - dfx canister create opencritique_backend
    - dfx canister create opnecritique_frontend
    - dfx build
    - dfx deploy
if cargo updated
    - cargo update
identity and principal commands
    - dfx identity list
    - dfx identity get-principal